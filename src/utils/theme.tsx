import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: "#B18E60",
            light: "#72706F",
            dark: "#43301D",
            contrastText: "#D1D5DD",
        },
    },
});

export default theme;